services:
  api:
    build:
      context: ./app
      dockerfile: Dockerfile
      target: production
    environment:
      NODE_ENV: production
      LOG_LEVEL: ${LOG_LEVEL:-info}
    volumes: []
    restart: always
